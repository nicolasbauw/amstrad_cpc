Fait, à confirmer sur le Mac (tout testé en une fois à la fin)

- F11 bloquée par macOS ("Afficher le bureau") : ajouté Cmd+Maj+C comme
  raccourci alternatif pour la console, en plus de F11 (qui reste la
  touche standard partout ailleurs). Commit 32b98eb.

- Bouton "plein écran" macOS grisé : la fenêtre principale n'était pas
  .resizable() — corrigé. Commit beffdaa.

- Icône à bords arrondis pas cohérente entre lancée/pas lancée sous macOS :
  set_window_icon ne fait plus rien sous macOS (SDL y détournait l'appel
  vers l'icône du Dock, non masquée par le squircle arrondi que macOS
  applique aux .icns des vraies applications — d'où l'icône carrée une
  fois lancée). Le cadre "dev build" recolore maintenant en rouge le cadre
  gris déjà présent dans l'artwork, au lieu d'en dessiner un nouveau au
  bord du canevas. Commit 5d8a4d1.

- RETURN scindée en deux boîtes de détection sur le clavier virtuel :
  fusionnées (écart entre les deux rectangles fermé, survol/appui allumant
  les deux ensemble). Commit c1619f9.

- Point rouge d'accès disque en overlay de l'écran de l'émulateur (comme
  F12) : ajouté, désactivable depuis le panneau F6 (case à cocher +
  show_disk_access_indicator dans config.toml, section [display]).
  Commit 8e7dd85.

- Texte caveats de la Formula Homebrew traduit en anglais (texte affiché à
  l'utilisateur, pas un commentaire). Commit 18cc9fd.

Packaging multi-plateforme (.github/workflows/build-packages.yml) — à reprendre

1) AppImage et MSI Windows : le workflow existe mais n'a jamais tourné pour
  de vrai (ni CI, ni test manuel). Déclencher un workflow_dispatch pour
  voir si ça compile/s'empaquette. Le MSI en particulier n'a jamais été
  testé sur une vraie machine Windows (personne n'en a une sous la main) —
  angle mort à garder en tête même une fois la CI verte.

2) Formula Homebrew (packaging/homebrew/bytebox.rb) : validée de bout en
  bout sur un vrai Mac (build, bundle .app, icône Dock/Launchpad correcte).
  Utilise actuellement une entrée `head` (branche dev) le temps que ce
  travail ne soit pas encore taggé — une fois dev mergée sur master et un
  nouveau tag créé :
    - retirer l'entrée `head` de la Formula (ou la garder pour les tests
      futurs, au choix)
    - recalculer le sha256 du nouveau tarball de tag et mettre à jour
      url/sha256
    - le job CI macOS (build-packages.yml) utilise encore l'ancienne
      logique (tap + install depuis le tag figé, pas --HEAD) : à revalider
      une fois qu'un tag à jour existe.

3) assets/bytebox_icon.icns : régénéré via iconutil sur le Mac (l'essai
  initial via png2icns/libicns — JPEG2000 par jasper, obsolète — donnait
  une icône grise/vide sous macOS). Si un jour l'icône source change,
  recommencer depuis le Mac (iconutil), pas depuis Linux.

- une fois toutes ces modifications faites, retester les packages, puis merger sur master et refaire une release 2.0 propre.
