- résidu de clignotement sous BMX Simulator (un sprite, rarement) : Caprice32 ne montre PAS ce clignotement avec la même disquette, donc pas un bug du jeu lui-même (contrairement à l'hypothèse initialement notée ici) — reste un bug d'émulation (timing/exécution) à trouver. Audit des catégories de cycles Z80 les plus à risque fait, rien trouvé. Reste, si on veut aller plus loin : désassembler BMX Simulator pour savoir quel code tourne autour de la trame qui clignote (voir doc/sprite-flicker.md, section "Résidu après correctif"). À RETESTER EN PRIORITÉ avec les deux correctifs CPU trouvés en résolvant WEC Le Mans : (a) le bug RST/interruption (voir doc/wec-le-mans-frozen-splash.md), et surtout (b) INC/DEC IYH/IYL absents du décodeur Z80 (0xFD24/25/2C/2D, voir doc/wec-le-mans-race-crash.md) — toute instruction utilisant les moitiés de IY était concernée, et ces registres servent couramment de registres 8 bits supplémentaires en code optimisé, donc candidat très plausible pour un sprite qui clignote. --> retesté, clignotement toujours existant, visible dès le départ de la course démo (qui démarre entre 55 et 60 secondes après l'affichage du menu du jeu, après son chargement).
- Discology : maintenant il se lance ! bug suivant, la copie ne fonctionne pas. Etapes pour reproduire:
1) quand le logiciel est lancé, faire flèche droit une fois (pour poitionner le curseur sur "copieur") attendre deux secondes puis valider avec "entrée"
2) On est alors sur le menu "Disquette" en surbrillance. Appuyer une fois sur entrée pour ouvrir ce menu, puis une fois flèche bas pour "copie intégrale".
3) Appuyer sur "entrée" pour valider ce choix, le logiciel demande alors d'insérer la disquette à copier dans le drive A. On appuie une seconde fois sur "entrée" pour valider, la copie est censée commencer, mais se bloque sur "lecture piste 00".

La résolution des bugs ci-dessus marquera -pour le moment- la fin de note campagne de chasse aux bugs : les logiciels du premier batch de tests seront 100% fonctionnels. D'autres tests sur d'autres logiciels auront lieu plus tard.

- quand l'émulateur est lancé via le raccourci desktop, il se lance donc sans terminal, et donc sans commandes à lancer depuis notre "console". Mais j'ai remarque que dans ce cas, le CPU du host s'emballe un peu. L'idée est donc :
1) dans ce cas, on ne lance pas la console (qui se fait via le crate::console::launch(m.cmd_channel.0.clone()).unwrap(); de machine.rs) (mais quid des prinln ?), car la majorité des gens, dans la majorité des cas, n'utiliseront pas la console et ses commandes plus réservées aux power users. De manière générale, dit autrement, il ne faut pas que notre émulateur dépende de cette console. A terme (TODO v2), on implémentera notre propre fenêtre qui remplacera le terminal d'ou est lancé l'émulateur, c'est une adhérence dont je souhaite me débarasser.
2) il nous faut donc (via la touche de fonction F6) pouvoir afficher une fenêtre SDL séparée, qui permet d'insérer des disquettes/cassettes, et paramétrer de façon visuelle ce qui est actuellement disponible dans le fichier de conf. Reprendre le code esthétique de la fenêtre de paramétrage de winuae/amiberry. Question subsidiaire : SDL permet-il d'ouvrir une fenêtre de sélection de fichier qui est agnostique au système (gnome, kde, windows, mac, etc...) ?
3) ma vision : je souhaite un émulateur qui puisse à la fois satisfaire ceux qui souhaitent simplement jouer, mais aussi les power users. Donc : capable de se lancer simplement du desktop et charger des jeux avec une interface simple. ET aussi de continuer à proposer les fonctions avancées actuellement disponibles via la console (en se désolidarisant du terminal de lancement de l'émulateur) et l'écran "machine status". Et de proposer aussi un shader CRT : ça devient commun, presque une norme dans les émulateurs. Je souhaite que ByteBox se distingue des autres en réunissant tous ces points. C'est presque plus qu'un todo, ça devient un document d'architecture UX qui va dicter des choix techniques.

- packaging (PKGBUILD AUR ?) : quand on s'y met, mettre à jour packaging/bytebox.desktop en conséquence (chemins actuellement en dur pour un usage personnel, Exec/Path pointent vers ~/Dev/amstrad_cpc).
