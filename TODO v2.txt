- Future v2 : On peut aussi évaluer la faisabilité / pertinence / effort de créer notre propre fenêtre de commandes, via par exemple lighostty (https://github.com/Uzaaft/libghostty-rs), ou avec un système de notre cru, affichable par la touche F11 (pour être cohérent avec la fenêtre de state via F12), qui remplacerait le terminal d'ou est lancé l'émulateur, c'est une adhérence dont je souhaite me débarasser.

- Future v2 : j'aimerais adapter le code pour avoir la possibilité d'un shader CRT en utilisant vulkan, je pense avoir vu des libs vulkan/sdl. On va se lancer dans la réalisation de cette modification structurelle, sur une branche à part. Le shader doit pouvoir s'adapter aux différentes résolutions (X1; X2, X3, fullscreen) pour avoir un rendu identique. Les scanlines ne doivent pas être trop prononcées ! elles doivent être visibles mais réalistes (le rendu CRT n'était pas si mal après tout). Les pixels doivent avoir un rendu plus rond, comme à l'époque sur un CRT.
